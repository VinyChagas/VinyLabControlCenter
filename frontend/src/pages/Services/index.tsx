import { PlaceholderPage } from '@/components/layout/PlaceholderPage';

export function ServicesPage() {
  return (
    <PlaceholderPage
      heading="Serviços"
      description="A listagem completa de serviços será conectada à API. O resumo dos principais serviços já está disponível no Dashboard."
    />
  );
}
